<Project>

  <PropertyGroup>
    <UseCurrentRuntimeIdentifier Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">true</UseCurrentRuntimeIdentifier>
    <NoWarn>CS1591</NoWarn>
  </PropertyGroup>

  <Import Project="../Directory.Build.props" />

</Project>